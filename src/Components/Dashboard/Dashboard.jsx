import { Title } from './Title/Title';

export const Dashboard = (props) => {
  return (
    <div style={{ width: '100%' }}>
      <h1 style={{ textAlign: 'center' }}>To be changed</h1>;
    </div>
  );
};
