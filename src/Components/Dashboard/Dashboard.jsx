import { Title } from './Title/Title';

export const Dashboard = (props) => {
  return (
    <div style={{ width: '100%' }}>
      <h1 style={{ textAlign: 'center' }}>{value}</h1>;
    </div>
  );
};
